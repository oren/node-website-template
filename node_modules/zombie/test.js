 var Browser = require('./lib/zombie');
var browser = new Browser();
browser.visit('http://zombie1.herokuapp.com', function(err, browser) {
  console.log("Loaded")
  browser.
  fill("#signup-username", "bob" + Math.random()).
  fill("#signup-password", "secret").
  pressButton("Sign Up!", function() {
  browser.wait(function() { 
    console.log("Done")
  })
  });
});
